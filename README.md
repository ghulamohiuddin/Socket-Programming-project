# Socket-Programming-project
A TCP socket programming project written in C that demonstrates client server communication. The project supports interactive message exchange between the client and server using Linux sockets.
